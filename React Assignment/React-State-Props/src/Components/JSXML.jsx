import React from "react";

const JSXML = () => {
  const Explaination =
    "JSX is a syntax extension for JavaScript. It allows you to write HTML elements in React and makes your code more readable.";
  return (
    <>
      <h2>Welcome to JSX</h2>
      <span>{Explaination}</span>
    </>
  );
};

export default JSXML;
